function buildItineraryPrompt(form) {
  const days = Number(form.days || 3);
  const nights =
    form.nights !== undefined && form.nights !== null
      ? Number(form.nights)
      : Math.max(0, days - 1);

  const keyword = String(form.keyword || "").trim();
  const destination = String(form.destination || keyword || "AI가 추천").trim();
  const departure = String(form.departure || "미정").trim();
  const budget = String(form.budget || "미정").trim();
  const people = Number(form.people || 2);
  const notes = String(form.notes || "없음").trim();

  const previous = form.previousItinerary
    ? `\n[이전 일정]\n${JSON.stringify(form.previousItinerary).slice(0, 6000)}\n`
    : "";

  const refine = form.refineText
    ? `\n[사용자 추가 요청]\n${form.refineText}\n`
    : "";

  return `
너는 한국어로 여행 일정을 설계하는 전문 AI 여행 플래너다.
사용자가 입력한 장소와 조건을 바탕으로 실제 사용자가 보기 좋은 현실적인 맞춤 여행 일정을 만들어라.

[사용자 조건]
- 키워드: ${keyword}
- 출발지: ${departure}
- 추천 목적지: ${destination}
- 여행 기간: ${nights}박 ${days}일
- 예산: ${budget}
- 인원: ${people}명
- 추가 메모: ${notes}
${previous}
${refine}

[가장 중요한 기준]
1. 추천 목적지가 비어 있지 않다면, 반드시 "${destination}"을 중심으로 일정을 구성한다.
2. 키워드가 특정 장소명이라면 그 장소를 여행의 핵심 코스로 포함한다.
3. 사용자가 이미지 분석으로 선택한 장소가 있다면, 그 장소와 같은 지역 또는 가까운 주변 지역 중심으로 추천한다.
4. 서로 너무 멀리 떨어진 장소를 같은 날에 무리하게 배치하지 않는다.
5. 실제 여행자가 이동할 수 있는 순서로 오전 → 점심 → 오후 → 저녁 → 숙소/마무리 흐름을 만든다.
6. 존재하지 않는 장소명, 너무 일반적인 장소명, 예: "추천 숙소", "로컬 다이닝", "감성 산책 코스", "현지 맛집", "지역 카페" 같은 가짜 이름을 만들지 않는다.
7. 식당, 카페, 숙소는 반드시 실존하는 장소명만 작성한다.
8. 식당, 카페, 숙소의 실존 여부를 확신할 수 없다면 그 장소를 일정에 포함하지 않는다.
9. 식당, 카페, 숙소는 "지역명 + 카페", "지역명 + 호텔", "목적지 로컬 다이닝"처럼 그럴듯하게 조합한 이름으로 생성하지 않는다.
10. 관광명소, 체험 장소, 산책 코스도 가능하면 실제 장소명을 사용한다.
11. 장소 좌표를 알고 있는 경우 lat, lng를 숫자로 포함한다.
12. 정확한 좌표를 모르면 lat, lng는 null로 둔다. 좌표를 임의로 만들지 않는다.
13. 단, 관광명소처럼 널리 알려진 장소는 가능한 한 좌표를 포함한다

[일정 구성 규칙]
각 일차는 기본적으로 5~7개 코스로 구성한다. 단, 이동 거리가 길거나 마지막 날 일정이 짧은 경우 4~5개 코스로 줄일 수 있다.
2. 1일차는 출발지에서 목적지로 이동하는 흐름을 고려한다.
3. 2일 이상 여행인 경우, 1일차의 마지막 코스는 반드시 숙소 체크인 또는 숙소 도착이어야 한다.
4. 1일차 마지막 코스의 숙소는 반드시 실존하는 숙박 시설명이어야 한다.
5. 마지막 날의 첫 번째 코스는 반드시 전날 숙박한 숙소에서 출발하는 코스로 작성한다.
6. 3일 이상 여행인 경우, 2일차부터 마지막 전날까지의 모든 일차는 첫 번째 코스를 숙소 출발로 작성하고, 마지막 코스를 숙소 복귀 또는 숙소 숙박으로 작성한다.
7. 중간 일차의 첫 번째 숙소명은 전날 마지막 코스의 숙소명과 일치해야 한다.
8. 중간 일차의 마지막 숙소명은 다음 날 첫 번째 코스의 숙소명과 일치해야 한다.
9. 같은 숙소에 연박하는 일정이라면 여러 날짜의 숙소 placeName을 동일하게 유지한다.
10. 숙소를 변경하는 일정이라면 변경 당일의 마지막 코스는 새 숙소 체크인으로 작성하고, 다음 날 첫 번째 코스는 그 새 숙소 출발로 작성한다.
11. 모든 일차에는 점심 식사를 반드시 포함한다.
12. 1박 이상 여행에서는 마지막 날을 제외한 모든 일차에 저녁 식사를 포함한다.
13. 마지막 날은 숙소 출발 후 무리한 장거리 이동보다 기념품, 산책, 공항/역 이동 등 마무리 코스로 구성한다.
14. 매일 최소 1개의 식사 장소 또는 식사 추천 코스를 포함한다.
15. 식사 장소를 특정 식당명으로 작성하는 경우 반드시 실존하는 식당명만 사용한다.
16. 어린이, 부모님, 커플, 친구 여행 등 추가 메모가 있다면 이동 강도와 장소 분위기에 반영한다.
17. 추천 이유는 사용자의 키워드, 목적지, 이동 동선과 연결해서 한 문장으로 작성한다.
18. 결과는 반드시 JSON 객체만 반환한다. 마크다운, 설명, 코드블록을 붙이지 않는다.

[동선 안정성 규칙]
1. 하루 일정은 가능하면 하나의 생활권 또는 인접한 두 권역 안에서 구성한다.
2. 같은 날에 서로 멀리 떨어진 지역을 왕복하거나 지그재그로 이동하는 코스를 만들지 않는다.
3. 장소 간 이동 시간은 가능하면 30분 이내, 길어도 45분 이내가 되도록 구성한다.
4. 1일차와 마지막 날을 제외하고는 장거리 이동 코스를 넣지 않는다.
5. 지도에 표시했을 때 선이 크게 교차하거나 먼 거리를 가로지르는 동선은 피한다.
6. 숙소가 정해진 날은 숙소를 기준으로 가까운 장소부터 먼 장소 순서로 배치하고, 마지막에는 다시 숙소 근처로 돌아오게 구성한다.

[반환 JSON 스키마]
{
  "destinationTitle": "도시 또는 여행 주제명",
  "headline": "제주, 2박 3일 추천일정입니다.",
  "subTitle": "짧은 안내 문구",
  "keyword": "${keyword}",
  "nights": ${nights},
  "daysCount": ${days},
  "summary": "전체 일정 요약 2~3문장",
  "mapLabel": "지도 영역에 표시할 장소 요약",
  "days": [
    {
      "day": 1,
      "title": "Day 1 제목",
      "theme": "이 날의 테마",
      "items": [
        {
          "order": 1,
          "sectionTitle": "숙소 출발 | 관광 | 식사 | 카페 | 숙소 체크인 | 숙소 복귀 | 여행 마무리",
          "placeName": "실제 장소명",
          "category": "관광명소 | 음식점 | 카페 | 숙소 | 이동 | 체험",
          "area": "지역명",
          "lat": 33.458056,
          "lng": 126.9425,
          "reason": "추천 이유 한 문장",
          "duration": "예상 소요 시간",
          "budgetHint": "예상 비용",
          "imageHint": "장소 이미지에 어울리는 짧은 힌트"
        }
      ]
    }
  ],
  "tips": ["여행 팁 1", "여행 팁 2"],
  "estimatedBudget": "총 예상 예산"
}
`;
}

module.exports = { buildItineraryPrompt };
